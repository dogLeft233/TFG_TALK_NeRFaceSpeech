# 重构

现在系统逻辑主要在app中  
是从fastapi_server重构来的

# 待解决问题

1. **视频生成后返回 index 会卡住** - generate 生成视频后返回 index 页面时出现卡顿/阻塞
2. **功能迁移不完整** - 可能有部分 fastapi_server 的功能未完全迁移到 app
3. **模型训练未实装** - 完整的模型训练功能未实现，目前只有角色训练功能
4. **语音输入功能未测试** - 语音输入并生成视频的功能需要测试验证
5. docker封装
6. 其他没看到的问题

